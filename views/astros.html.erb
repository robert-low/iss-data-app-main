<%# Data fetched from astros.json. Iterating through people key, value is array of hashes. %>
<div class="centre-container">
  <h1>Who are the astronauts currently in space? 👩‍🚀</h1>

  <p>There are <strong><%= data['number'] %></strong> astronauts in space right now.</p>

  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Craft</th>
      </tr>
    </thead>
    <tbody>
      <% data['people'].each do |person| %>
        <tr>
          <td><%= person['name']%></td>
          <td><%= person['craft'] %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <img src="images/snowy.png" alt="snowy in space" style="width: 300px; height: auto; margin: 12px 12px">

  <a class="home-btn" href='/'>Back to 🏠</a>

</div>





<%#
1. Build a page to show the current astronauts that are in space, using the OpenNotify#astros method.
  - Create new .erb file for displaying astros.
  - Provide route in index file to astros
  - In app.rb fetch the astros api data using pre-defined method in opennotify module, save to variable.
  - Display fetched data in astros.erb
    > Data structure? {number: 10, people: [ { name:, craft: } ] }
    > Iterate through people to display astro name and craft
    > Display this data in table
 %>
