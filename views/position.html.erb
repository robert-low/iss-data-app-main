<%# View page displaying location of ISS using #get /position in app.rb.
%>

<div class="centre-container">
  <h1>Where is the ISS? 🛰️</h1>

  <p>
    The longitude of the ISS is <strong><%= data['iss_position']['longitude']%></strong>
    and the latitude is <strong><%= data['iss_position']['latitude'] %></strong>.
  </p>

  <img class="yellow-squiggle" style="width: 460px; height: auto;"
  src="/images/yellow_squiggle.jpg" alt="Yellow underline">

  <p>
    This observation was made on <%= Time.at(data['timestamp']).strftime("%m/%d/%Y")%>
    at <%= Time.at(data['timestamp']).strftime("%I:%M%p") %>
    <%# #strftime for readability %>
  </p>

  <div class="btn-container">
    <a class="json-btn" href='http://localhost:4567/iss_position.json'>View as JSON {}</a>
  </div>

  <div class="btn-container">
    <a class="home-btn" href='/'>Back to 🏠</a>
  </div>

</div>
